huggingface-hub>=0.21.0
pandas>=2.0.0
pyarrow>=14.0.0
duckdb>=0.9.0

# fix crítico: versão fixa evita builds inconsistentes
pybloomfiltermmap3==0.5.3

rich>=13.0.0
psutil>=5.9.0

# libtorrent no CI pode falhar dependendo do ambiente,
# mas mantemos para compatibilidade
libtorrent
