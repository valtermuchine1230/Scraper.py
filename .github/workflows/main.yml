name: Minerador de Alta Performance
on: [workflow_dispatch]

jobs:
  mineracao:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Instalar Dependências
        run: pip install psycopg[binary] libtorrent
      - name: Executar Minerador
        run: python minerador.py
